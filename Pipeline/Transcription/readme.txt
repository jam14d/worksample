Pipeline that converts a random string input into a DNA sequence, then converts the DNA sequence to RNA.


Outputs can be saved as a .txt.

To run your pipeline, execute main.py. 
Ensure all these files are in the same directory.
